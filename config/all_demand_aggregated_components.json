{
  "_note": "Human-owned record of which LEAP demand sectors are included in 'All demand aggregated' (i.e. have no separately modelled LEAP detail yet). Consumed by codebase/mapping_tools/source_branch_preflight.py: check_all_demand_aggregated_overlap() warns when the aggregate and a still-configured included sector are both non-zero for the same economy/period; get_demand_sectors_without_detail() exposes the resolved per-economy list to downstream consumers such as the dashboard workflow, which skips rendering demand-sector pages that would otherwise show no LEAP detail.",
  "aggregated_branch": "All demand aggregated",
  "components": [
    {
      "component_branch": "Buildings",
      "detailed_branches": ["Buildings"],
      "detail_activation": "all_present",
      "include_by_default": true,
      "note": "Declared as included in All demand aggregated. When the configured detailed branch is present, only this placeholder component is suppressed in mapping working data.",
      "economy_overrides": {}
    },
    {
      "component_branch": "Road",
      "detailed_branches": ["Freight road", "Passenger road"],
      "detail_activation": "all_present",
      "include_by_default": true,
      "note": "Declared as included in All demand aggregated. When both Freight road and Passenger road are present, only the Road placeholder component is suppressed in mapping working data.",
      "economy_overrides": {}
    },
    {
      "component_branch": "Industry",
      "detailed_branches": ["Industry"],
      "detail_activation": "all_present",
      "include_by_default": true,
      "note": "Declared as included in All demand aggregated. When the configured detailed branch is present, only this placeholder component is suppressed in mapping working data.",
      "economy_overrides": {}
    },
    {
      "component_branch": "Other sector",
      "detailed_branches": ["Other sector"],
      "detail_activation": "all_present",
      "include_by_default": true,
      "note": "Declared as included in All demand aggregated. When the configured detailed branch is present, only this placeholder component is suppressed in mapping working data.",
      "economy_overrides": {}
    },
    {
      "component_branch": "Transport non road",
      "detailed_branches": ["Transport non road"],
      "detail_activation": "all_present",
      "include_by_default": true,
      "note": "Declared as included in All demand aggregated. When the configured detailed branch is present, only this placeholder component is suppressed in mapping working data.",
      "economy_overrides": {}
    },
    {
      "component_branch": "International transport",
      "detailed_branches": ["International transport"],
      "detail_activation": "all_present",
      "include_by_default": true,
      "note": "Declared as included in All demand aggregated. When the configured detailed branch is present, only this placeholder component is suppressed in mapping working data; international transport retains its source sign.",
      "economy_overrides": {}
    }
  ]
}
